{
  "name": "the-needs-audit",
  "version": "1.0.0",
  "description": "The Needs Audit — TNA practice rounds, workbench, learner + trainer app",
  "private": true,
  "scripts": {
    "migrate": "node db/migrate.js",
    "start": "node db/migrate.js && node server.js",
    "dev": "node server.js",
    "smoke": "node scripts/smoke-test.js"
  },
  "dependencies": {
    "cookie-session": "^2.1.0",
    "express": "^4.19.2",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=18"
  }
}
